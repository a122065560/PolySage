# ============================================================
# PolySage（聚慧）多平台发布构建工作流
# ------------------------------------------------------------
# 触发方式：
#   1. 手动触发 (workflow_dispatch) —— 输入版本号
#   2. 推送 v* 标签自动触发
# 构建平台：macOS (ARM64) + Windows (x64)
# 产物：PolySage-{version}-macOS.dmg / PolySage-{version}-Windows.exe
# ============================================================
name: 发布构建

on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (如 v1.0.0)'
        required: true
        type: string
  push:
    tags:
      - 'v*'

env:
  VERSION: ${{ github.event_name == 'workflow_dispatch' && inputs.version || github.ref_name }}

jobs:
  # ============================================================
  # macOS 构建任务 (ARM64)
  # 使用 build_dmg.sh 完整构建流程（含 Qt6 符号链接等关键步骤）
  # ============================================================
  build-macos:
    runs-on: macos-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: app/requirements.txt

      - name: 缓存 Playwright 浏览器
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/ms-playwright
          key: macos-playwright-chromium-${{ hashFiles('app/requirements.txt') }}
          restore-keys: macos-playwright-chromium-

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r app/requirements.txt pyinstaller==6.3.0

      - name: 安装 Playwright Chromium
        run: playwright install chromium

      - name: 构建 macOS .app + .dmg（build_dmg.sh）
        working-directory: app
        env:
          GITHUB_ACTIONS: 'true'
          DMG_NAME: 聚慧-${{ env.VERSION }}-macOS.dmg
        run: bash build_dmg.sh

      - name: 上传 macOS DMG
        uses: actions/upload-artifact@v4
        with:
          name: macos-dmg
          path: app/dist/聚慧-${{ env.VERSION }}-macOS.dmg
          retention-days: 30

  # ============================================================
  # Windows 构建任务 (x64)
  # ============================================================
  build-windows:
    runs-on: windows-latest
    env:
      CERTIFICATE_BASE64: ${{ secrets.WINDOWS_CERTIFICATE_BASE64 }}
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: app/requirements.txt

      - name: 缓存 Playwright 浏览器
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\ms-playwright
          key: windows-playwright-chromium-${{ hashFiles('app/requirements.txt') }}
          restore-keys: windows-playwright-chromium-

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r app/requirements.txt pyinstaller==6.3.0

      - name: 安装 Playwright Chromium
        run: playwright install chromium

      - name: PyInstaller 打包
        working-directory: app
        run: pyinstaller --noconfirm PolySage.spec

      - name: 配置 Inno Setup
        run: echo "C:\Program Files (x86)\Inno Setup 6" >> $GITHUB_PATH

      - name: 生成 Windows 安装包
        run: iscc /DMyAppVersion=${{ env.VERSION }} installer.iss

      - name: 代码签名
        if: ${{ env.CERTIFICATE_BASE64 != '' }}
        shell: pwsh
        env:
          CERT_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        run: |
          $certBytes = [System.Convert]::FromBase64String($env:CERTIFICATE_BASE64)
          [System.IO.File]::WriteAllBytes("cert.pfx", $certBytes)
          $signtool = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\signtool.exe" |
            Sort-Object FullName -Descending | Select-Object -First 1
          & $signtool.FullName sign /f cert.pfx /p $env:CERT_PASSWORD `
            /fd sha256 /tr http://timestamp.digicert.com /td sha256 `
            "聚慧-${{ env.VERSION }}-Windows.exe"

      - name: 上传 Windows 安装包
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: 聚慧-${{ env.VERSION }}-Windows.exe
          retention-days: 30

  # ============================================================
  # 发布 Release
  # ============================================================
  release:
    runs-on: ubuntu-latest
    needs: [build-macos, build-windows]
    permissions:
      contents: write
    steps:
      - name: 下载构建产物
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: 列出产物文件
        run: find artifacts -type f

      - name: 发布 Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          name: ${{ env.VERSION }}
          files: |
            artifacts/macos-dmg/*.dmg
            artifacts/windows-exe/*.exe
          generate_release_notes: true
          draft: false
          prerelease: false
          fail_on_unmatched_files: true
